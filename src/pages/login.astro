---
import Layout from "../layouts/Layout.astro";
import "../styles/tailwind.css"

// login.astro
// Pagina di login in stile dark/red per la admin dashboard

// Meta data della pagina
const pageTitle = "Login - Admin Dashboard";
---
<Layout>
<style>
    body {
        font-family: 'Inter', sans-serif;
    }
    
    /* Animazione pulse personalizzata */
    @keyframes pulse-red {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }
    
    .animate-pulse-red {
        animation: pulse-red 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    
    /* Effetto shine per il pulsante */
    .btn-shine {
        position: relative;
        overflow: hidden;
    }
    
    .btn-shine::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
        transition: left 0.7s ease-in-out;
    }
    
    .btn-shine:hover::after {
        left: 100%;
    }
</style>
<div class="bg-[rgb(19,19,19)] text-white min-h-screen flex items-center justify-center p-4">
    
    <!-- Background decoration -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-0 left-0 w-full h-full bg-linear-to-br from-red-600/5 via-transparent to-transparent"></div>
        <div class="absolute bottom-0 right-0 w-96 h-96 bg-red-600/10 rounded-full blur-3xl"></div>
        <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-200 h-200 bg-red-600/5 rounded-full blur-3xl"></div>
    </div>
    
    <!-- Main container -->
    <div class="w-full max-w-md relative z-10">
        
        <!-- Logo e titolo -->
        <div class="text-center mb-8">
            <div class="inline-flex items-center justify-center space-x-2 mb-4">
                <div class="w-3 h-3 bg-red-600 rounded-full animate-pulse-red"></div>
                <span class="text-white font-bold text-2xl tracking-wider">DARK GUI</span>
            </div>
            <h1 class="text-3xl font-bold text-white mb-2">Benvenuto</h1>
            <p class="text-gray-500 text-sm">Accedi alla dashboard amministrativa</p>
        </div>
        
        <!-- Login Card -->
        <div class="bg-black border border-red-900/30 rounded-2xl p-8 shadow-2xl shadow-black/50">
            
            <!-- Form di login -->
            <form class="space-y-6" method="POST" action="/api/login">
                
                <!-- Campo Username/Email -->
                <div class="space-y-2">
                    <label for="username" class="block text-sm font-medium text-gray-300">
                        Username o Email
                        <span class="text-red-500 text-xs ml-1">*</span>
                    </label>
                    <div class="relative">
                        <input
                            type="text"
                            id="username"
                            name="username"
                            placeholder="Inserisci username o email"
                            required
                            class="w-full pl-10 pr-4 py-3 bg-[rgb(19,19,19)] border border-red-900/30 rounded-xl
                                   text-white placeholder-gray-600
                                   focus:outline-none focus:border-red-600 focus:ring-1 focus:ring-red-600/30
                                   transition-all duration-300"
                        />
                    </div>
                </div>
                
                <!-- Campo Password -->
                <div class="space-y-2">
                    <div class="flex items-center justify-between">
                        <label for="password" class="block text-sm font-medium text-gray-300">
                            Password
                            <span class="text-red-500 text-xs ml-1">*</span>
                        </label>
                        <a href="/forgot-password" class="text-xs text-red-600 hover:text-red-500 transition-colors">
                            Password dimenticata?
                        </a>
                    </div>
                    <div class="relative">
                        <input
                            type="password"
                            id="password"
                            name="password"
                            placeholder="Inserisci password"
                            required
                            class="w-full pl-10 pr-4 py-3 bg-[rgb(19,19,19)] border border-red-900/30 rounded-xl
                                   text-white placeholder-gray-600
                                   focus:outline-none focus:border-red-600 focus:ring-1 focus:ring-red-600/30
                                   transition-all duration-300"
                        />
                    </div>
                </div>
                
                <!-- Checkbox Remember me -->
                <div class="flex items-center justify-between">
                    <label class="flex items-center space-x-2 cursor-pointer">
                        <input
                            type="checkbox"
                            name="remember"
                            class="w-4 h-4 bg-[rgb(19,19,19)] border border-red-900/30 rounded 
                                   text-red-600 focus:ring-red-600/30 focus:ring-offset-0
                                   transition-colors duration-300"
                        />
                        <span class="text-sm text-gray-400">Ricordami</span>
                    </label>
                    
                    <!-- Indicatore di sicurezza -->
                    <div class="flex items-center space-x-1 text-xs text-gray-600">
                        <span class="w-1.5 h-1.5 bg-green-500 rounded-full"></span>
                        <span>Connessione sicura</span>
                    </div>
                </div>
                
                <!-- Pulsante di login -->
                <button
                    type="submit"
                    class="btn-shine w-full px-6 py-3.5 bg-red-600 text-white font-medium rounded-xl
                           hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-600/50 
                           focus:ring-offset-2 focus:ring-offset-black
                           transition-all duration-300 mt-6
                           flex items-center justify-center space-x-2 group"
                >
                    <span>Accedi alla Dashboard</span>
                    <span class="text-lg group-hover:translate-x-1 transition-transform duration-300">→</span>
                </button>
                
                <!-- Separatore -->
                <div class="relative my-6">
                    <div class="absolute inset-0 flex items-center">
                        <div class="w-full border-t border-red-900/30"></div>
                    </div>
                    <div class="relative flex justify-center text-xs">
                        <span class="px-2 bg-black text-gray-500">accesso riservato</span>
                    </div>
                </div>
                
                <!-- Link per registrazione (opzionale) -->
                <p class="text-center text-sm text-gray-500">
                    Non hai un account?
                    <a href="/register" class="text-red-600 hover:text-red-500 transition-colors font-medium">
                        Contatta l'amministratore
                    </a>
                </p>
            </form>
        </div>
        
        <!-- Footer con informazioni -->
        <div class="mt-8 text-center">
            <p class="text-xs text-gray-700">
                © 2026 DARK GUI - Admin Panel v2.0
            </p>
            <div class="flex items-center justify-center space-x-4 mt-2 text-[10px] text-gray-800">
            </div>
        </div>
        
        <!-- Messaggio di errore (esempio, nascosto di default) -->
        <div class="mt-4 p-4 bg-red-600/10 border border-red-600/30 rounded-lg hidden">
            <p class="text-red-500 text-sm flex items-center">
                <span class="mr-2">⚠️</span>
                Credenziali non valide. Riprova.
            </p>
        </div>
    </div>
</div>
</Layout>